package jp.co.jjs.java_seminar.self_study.q3_7;

public class ec {
    public static void main(String[] args) {

        int x;
        // 3.7_1
        for (x = 0; x < 100; x++)
            System.out.println(x);
        // 0～99までの数を出力

        // 3.7_2
        // !(10 == 9) この場合TRUEとなる

        // 3.7_3
        // ((a >= 0) || (c > b / c))
        // aが0未満の場合、右が評価対象となる

        // 3.7_4

        if (x >= 0 && x <= 100) {
        }
        if (x == 0 || x <= 100) {
        }

    }
}
